<h1>The courses are</h1>
Here we want a ul list with the instructor, coursenum, title, term
<hr>
<table class="table table-bordered table-striped">
    <thead>
        <tr>
            <th>First Name</th>
            <th>Last Name #</th>
            <th>PlayerID</th>
            <th>TeamID</th>
        </tr>
    </thead>
    <tbody></tbody>
    <% for (course of courses) {
        const inst = course['firstName'];
         %>
      <tr class="mb-2"> 
        <td><%= semester[term%10] %></td>
        <td>
            <a href="/courses/show/<%=course._id %>">
            <%=  course['playerID'] %>
            <%=  course['teamID'] %>  

            </a>
        </td>

        <td>
            <strong><%= course.name %></strong>
        </td>

        <td>
            <%=course.enrolled %>/<%=course.limit || 999 %>/<%= course.waiting %>
        </td>


        <td>
          <%= inst[0]+" "+inst[1]%> <br> <%= " ("+inst[2]+")" %>
        </td>
    </tr>
    <% } %>
    </tbody>
</table>

<pre>
    <!-- 
        <%= JSON.stringify(courses,null,5) %> 
    -->
</pre>
