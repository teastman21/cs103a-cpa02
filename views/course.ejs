<div class="bg-light m-5 p-2 border border-primary border-2">
    <div class="">
        <span>
            <%= course.firstName %> 
            <%= course.lastName %> 
            <%= course.playerID %>
            <%= course.teamID %>
        </span>
        <strong><%= course.name %></strong>

        
    </div>
    <div class="mb-1">
        <%=course.instructor[0] %>
        <%=course.instructor[1] %>
        (<%=course.instructor[2] %>)
    </div>
    <div><a href="/addCourse/<%= course['playerID'] %>">(add to schedule)</a></div>
    <div class="mb-1">

</div>

<hr>

<pre>
    <%= JSON.stringify(course,null,5) %>
</pre>
-->
